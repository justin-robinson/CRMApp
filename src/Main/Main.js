import React, { Component } from 'react';
import Router from '../Router/Router';
import './Main.scss';

class Main extends Component {
  render() {
    return (
      <main>
        <Router/>
      </main>
    );
  }
}

export default Main;